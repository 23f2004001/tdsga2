name: Email Test Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f2004001@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final step
        run: echo "Workflow completed"
